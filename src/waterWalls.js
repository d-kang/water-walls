const waterWalls = (walls) => {
  const largestSum = 0;
  const largestPool = [];
  const tempSum = 0;
  const tempPool = [];

  const head = 0;
  const current = 1;
  const tail = 2;

  if (walls.length === 0 || walls.length === 1) {
    return [];
  }
  while(notDone) {

  }
}

module.exports = {
  waterWalls,
}
